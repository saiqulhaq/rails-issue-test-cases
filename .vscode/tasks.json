{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Create project",
            "type": "shell",
            "command": "rails new project -G --skip && cd project && bundle install"
        }, 
        {
            "label": "Delete project",
            "type": "shell",
            "command": "cd project && rm -rf * && rm -f .ruby-version"
        }, 
    ], 
}